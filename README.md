# gemini-cli

Minimal prep repo for `@google/gemini-cli`.

This repo installs the published npm package with Bun and applies a small set
of runtime patches after install. `nixpkg-gemini` consumes this prepared package
shape and stays packaging-only.
